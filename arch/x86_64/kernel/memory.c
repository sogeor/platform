#include <kernel/memory.h>
#include <libx/x86_64/mmu.h>

ptr memory_allocate_aligned(umax alignment, umax size)
{
    // TODO: do it
    return NULL_PTR;
}

ptr memory_allocate_unaligned(umax size)
{
    // TODO: do it
    return NULL_PTR;
}

ptr memory_reallocate(ptr memory, umax size)
{
    // TODO: do it
    return NULL_PTR;
}

void memory_free(ptr memory)
{
    // TODO: do it
}
